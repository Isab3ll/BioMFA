<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/biomfa_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/biomfa_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BioMFA"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
                android:name=".TypeCodeActivity"
                android:exported="true">
        </activity>

        <activity
                android:name=".ScanImplantActivity"
                android:exported="true">
        </activity>

        <activity
            android:name=".InfoActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ScanRfidActivity"
            android:label="Scan RFID Tag"
            android:exported="true">
        </activity>

        <activity
            android:name=".ShowDataActivity"
            android:label="Scanned Data"
            android:exported="true">
        </activity>

    </application>

</manifest>